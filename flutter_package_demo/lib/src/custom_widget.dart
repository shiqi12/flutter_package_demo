part of '../flutter_package_demo.dart';

class CustomWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Image.asset("images/test-1"),
      width: 300,
      height: 300,
      color: Colors.yellow,
    );
  }
}
